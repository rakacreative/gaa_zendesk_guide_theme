<section class="section hero hero-community">
    <div class="hero-overlay"></div>
    <div class="hero__inner layout-container container-fluid container-sm">
        <h1 class="hero__heading">Welcome to your virtual seafood community</h1>
        <h2 class="visibility-hidden">{{ t 'search' }}</h2>
        {{search submit=false class='search search-full' scoped=settings.scoped_community_search}}
    </div>
</section>

<div class="breadcrumbs-wrapper">
    <div class="layout-container container-fluid">
        <nav
            class="sub-nav"
            aria-label="breadcrumb"
        >
            {{breadcrumbs}}
        </nav>
    </div>
</div>

<div class="community-intro-blurb">
    <div class="layout-container container-fluid container-sm">
        <p>This is where the connections happen – select a topic below and start asking questions, networking with others, and creating discussions.</p>
    </div>
</div>

<div class="layout-container container-fluid">
    <header class="section-header community-header">
        <h2 class="title">
            {{t 'community_topics'}}
        </h2>
    </header>
</div>

{{#unless topics}}
    <p>{{t 'no_content'}}</p>
{{/unless}}

<section class="block">
    <div class="layout-container container-fluid">
        <div
            id="main-content"
            class="blocks topics"
        >
            <ul class="blocks-list topics-list">
                {{#each topics}}
                    <li class="blocks-item topics-item {{#if internal}}blocks-item-internal{{/if}}">
                        <a
                            href="{{url}}"
                            class="blocks-item-link"
                        >
                            <span class="blocks-item-title">
                                {{name}}

                                {{#if internal}}
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        width="16"
                                        height="16"
                                        focusable="false"
                                        viewBox="0 0 16 16"
                                        class="icon-lock"
                                        title="{{t 'internal'}}"
                                    >
                                        <rect
                                            width="12"
                                            height="9"
                                            x="2"
                                            y="7"
                                            fill="currentColor"
                                            rx="1"
                                            ry="1"
                                        />
                                        <path
                                            fill="none"
                                            stroke="currentColor"
                                            d="M4.5 7.5V4a3.5 3.5 0 017 0v3.5"
                                        />
                                    </svg>
                                {{/if}}
                            </span>
                            <span class="blocks-item-description">{{excerpt description}}</span>
                            <ul class="meta-group">
                                <li class="meta-data">{{t 'post_count' count=post_count}}</li>
                                <li class="meta-data">{{t 'follower_count' count=follower_count}}</li>
                            </ul>
                        </a>
                    </li>
                {{/each}}
            </ul>
        </div>
    </div>
</section>

{{pagination}}

{{#if featured_posts}}
    <section class="section community-featured-posts">
        <div class="layout-container container-fluid">
            <h2 class="title">{{t 'featured_posts'}}</h2>
            <ul class="promoted-articles">
                {{#each featured_posts}}
                    <li class="promoted-articles-item">
                        <a href="{{url}}">
                            {{title}}
                        </a>
                    </li>
                {{/each}}
            </ul>
        </div>
    </section>
{{/if}}

<section class="section community-activity activity">
    <div class="layout-container container-fluid">
        {{#if help_center.community_enabled}}
            {{recent_activity scope='community'}}
        {{/if}}
    </div>
</section>

<section class="section community-footer section-cta block-columns bg-catskill">
    <div class="layout-container container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="content-cms">
                    <h2 class="community-footer-title">{{t 'suggest_new_post'}}</h2>
                    {{#link 'new_post' class='btn btn-primary'}}
                        Create a new post
                    {{/link}}
                </div>
            </div>
        </div>
    </div>
</section>
